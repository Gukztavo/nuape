<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NAI/NUAPE - Sistema Acadêmico</title>
    <link rel="icon" href="assets/img/nuape.png" type="image/png" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="assets/css/styles.css" />
  </head>

  <body>
    <div class="container mt-5 text-center">
      <img
        src="assets/img/logo.png"
        alt="Logo UTFPR"
        class="img-fluid"
        style="max-width: 300px; margin-bottom: 20px"
      />
      <h1>NAI/NUAPE - Sistema Acadêmico</h1>
      <a class="btn btn-primary" href="export.php" role="button">Exportar para CSV</a>  
      <table class="table table-bordered mt-4">
        <thead>
          <tr>
            <th>ID</th>
            <th>RA</th>
            <th>Nome do Aluno</th>
            <th>Nome do Professor</th>
            <th>Recebeu Email</th>
            <th>Ciente</th>
            <th>Data de Confirmação</th>
          </tr>
        </thead>
        <tbody>
          {% for confirmation in confirmations %}
          <tr>
              <td>{{ confirmation.id|e }}</td>
              <td>{{ confirmation.ra|e }}</td>
              <td>{{ confirmation.aluno|e }}</td>
              <td>{{ confirmation.professor|e }}</td>
              <td>{{ confirmation.email_received|e }}</td>
              <td>{{ confirmation.acknowledged|e }}</td>
              <td>{{ confirmation.confirmation_date|e }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
